{
  "name": "wisata-masa-lalu",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:world-art": "node scripts/generate-world-raster.mjs",
    "build": "npm run generate:world-art && vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "audit:editorial": "node scripts/editorial-report.mjs",
    "deploy:worker": "wrangler deploy"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "@tailwindcss/vite": "^4.0.0",
    "sharp": "^0.34.3",
    "tailwindcss": "^4.0.0",
    "vite": "^6.1.0",
    "wrangler": "^4.0.0"
  }
}